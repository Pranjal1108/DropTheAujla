import { ChainId } from '../constants';
import { TokenAmount } from './currency-amount';
import { Token } from './currency';
import { BigNumber } from '@ethersproject/bignumber';
import { DateTime } from 'luxon';
import Decimal from 'decimal.js-light';
export declare class KpiToken extends Token {
    readonly kpiId: string;
    readonly totalSupply: TokenAmount;
    readonly oracle: string;
    readonly question: string;
    readonly lowerBound: BigNumber;
    readonly higherBound: BigNumber;
    readonly finalProgress: BigNumber;
    readonly expiresAt: DateTime;
    readonly finalized: boolean;
    readonly kpiReached: boolean;
    readonly creator: string;
    readonly collateral: TokenAmount;
    readonly fee: TokenAmount;
    constructor(chainId: ChainId, address: string, symbol: string, name: string, kpiId: string, totalSupply: BigNumber, oracle: string, question: string, lowerBound: BigNumber, higherBound: BigNumber, finalProgress: BigNumber, expiresAt: DateTime, finalized: boolean, kpiReached: boolean, creator: string, collateral: TokenAmount, fee: TokenAmount);
    get progressPercentage(): Decimal;
}
