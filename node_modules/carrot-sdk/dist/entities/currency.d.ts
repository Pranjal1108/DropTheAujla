import { BigNumber } from '@ethersproject/bignumber';
import { ChainId } from '../constants';
export declare class Currency {
    readonly decimals: BigNumber;
    readonly symbol: string;
    readonly name: string;
    static ETHER: Currency;
    constructor(decimals: BigNumber, symbol: string, name: string);
    static getNative(chainId: ChainId): Currency;
    equals(other: Currency): boolean;
}
export declare class Token extends Currency {
    readonly chainId: ChainId;
    readonly address: string;
    constructor(chainId: ChainId, address: string, decimals: BigNumber, symbol: string, name: string);
    equals(other: Token): boolean;
}
