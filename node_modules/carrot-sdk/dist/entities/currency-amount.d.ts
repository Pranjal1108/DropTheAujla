import { Currency, Token } from './currency';
import { BigNumber } from '@ethersproject/bignumber';
import { Fraction } from './fraction';
export declare class CurrencyAmount extends Fraction {
    readonly currency: Currency;
    constructor(currency: Currency, amount: BigNumber | Fraction);
    plus(other: CurrencyAmount): CurrencyAmount;
    minus(other: CurrencyAmount): CurrencyAmount;
    multiply(other: CurrencyAmount): CurrencyAmount;
}
export declare class TokenAmount extends CurrencyAmount {
    readonly token: Token;
    constructor(token: Token, amount: BigNumber | Fraction);
}
